{
  "name": "pilar-api",
  "version": "1.0.0",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && mv client/build ./build"
  }
}
